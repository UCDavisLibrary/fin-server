
version: '2'

###
# Demo Fin Server from deployed DockerHub container images
#
# To run create a .env file in the same directory as this docker-compose
# file and set the following environmental variables:
#    - JWT_SECRET=[server secret]
#    - JWT_ISSUER=[your domain, eg library.ucdavis.edu]
#    - FIN_URL=[application url, http://mydams.org or http://localhost:3000 for testing]
###
services:
  
  ###
  # Font End NodeJS Application Server
  ###
  server:
    image: ucdlib/fin-server:1.0.3
    volumes:
      - ./frames:/etc/fin/frames
      - ./default-services.js:/etc/fin/default-services.js
    ports:
      - 3000:3001
    environment:
      - JWT_SECRET=${JWT_SECRET}
      - JWT_ISSUER=${JWT_ISSUER}
      - FIN_URL=${FIN_URL}
    depends_on:
      - fcrepo
      - redis

  ###
  # Fedora Repository
  ###
  fcrepo:
    image: ucdlib/fin-fcrepo:v1.0.0
    volumes:
      - fedora-data:/var/lib/jetty/fedora-data
    environment:
      - JWT_SECRET=${JWT_SECRET}
      - JWT_ISSUER=${JWT_ISSUER}
      - FIN_URL=${FIN_URL}

  ###
  # Session, admin store
  ###
  redis:
    image: redis:3.2
    volumes:
      - redis-data:/data

  ###
  # UC DAMS Client UI
  ###
  ucd-lib-client:
    image: ucdlib/fin-ucd-lib-client:0.1.0
    environment:
      - JWT_SECRET=${JWT_SECRET}
      - JWT_ISSUER=${JWT_ISSUER}
      - FIN_URL=${FIN_URL}
      - FIN_ENV=prod
    depends_on:
      - elasticsearch

  ###
  # Search
  ###
  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:5.4.1
    environment:
      - bootstrap.memory_lock=true
      - "ES_JAVA_OPTS=-Xms512m -Xmx512m"
    ulimits:
      memlock:
        soft: -1
        hard: -1
    volumes:
      - es-data:/usr/share/elasticsearch/data
      - ./elasticsearch.yml:/usr/share/elasticsearch/config/elasticsearch.yml
    depends_on:
      - server

  ###
  # Trusted Proxy for Services
  # required for loris service
  ###
  trustedproxy:
    image: ucdlib/fin-trusted-proxy:1.0.0
    depends_on:
      - elasticsearch
    environment:
      - JWT_SECRET=${JWT_SECRET}
      - JWT_ISSUER=${JWT_ISSUER}

  ###
  # IIIF Service
  ###
  loris:
    image: ucdlib/fin-loris-service:v0.0.2
    depends_on:
      - server
    volumes:
      - loris-data:/usr/local/share/images/loris

  
  ###
  # ES Indexer
  ###
  essync:
    image: ucdlib/fin-essync-service:0.1.0
    environment:
      - JWT_SECRET=${JWT_SECRET}
      - JWT_ISSUER=${JWT_ISSUER}
      - FIN_URL=${FIN_URL}
    depends_on:
      - server

  ###
  # Serialize Fcrepo to disk
  ###
  serialization:
    image: ucdlib/fin-serialization-service:1.0.0
    environment:
      - JWT_SECRET=${JWT_SECRET}
      - JWT_ISSUER=${JWT_ISSUER}
      - FIN_URL=${FIN_URL}
    depends_on:
      - server

  ###
  # CAS AuthenticationService
  ###
  cas:
    image: ucdlib/fin-cas-service:0.0.1
    environment:
      - JWT_SECRET=${JWT_SECRET}
      - JWT_ISSUER=${JWT_ISSUER}
      - FIN_URL=${FIN_URL}
    depends_on:
      - server

  ###
  # Basic Username/Password AuthenticationService
  ###
  basic-auth:
    image: ucdlib/fin-basic-auth:0.0.1
    environment:
      - JWT_SECRET=${JWT_SECRET}
      - JWT_ISSUER=${JWT_ISSUER}
      - FIN_URL=${FIN_URL}
    depends_on:
      - server

###
# Docker data volumes
###
volumes:
  fedora-data:
    driver: local
  fedora-serialized-data:
    driver: local
  es-data:
    driver: local
  redis-data:
    driver: local
  loris-data:
    driver: local